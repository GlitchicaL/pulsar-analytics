services:
  frontend:
    build:
      args:
        - NODE_ENV=development
      context: .
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: npm run dev
    ports:
      - 3000:3000
    volumes:
      - .:/app
      - /app/node_modules # Avoid overwriting node_modules with host's version
